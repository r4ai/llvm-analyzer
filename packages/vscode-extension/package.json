{
  "name": "llvm-analyzer-vscode",
  "displayName": "LLVM IR",
  "version": "0.0.1",
  "private": true,
  "description": "LLVM IR language support with syntax highlighting and LSP features.",
  "categories": [
    "Programming Languages"
  ],
  "license": "MIT",
  "publisher": "r4ai",
  "main": "./dist/extension.js",
  "scripts": {
    "build": "node esbuild.mjs",
    "package": "pnpm build && vsce package --no-dependencies --allow-missing-repository --out llvm-analyzer-vscode.vsix",
    "test:e2e": "pnpm build && node test/runTest.mjs",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@llvm-analyzer/language-server": "workspace:*",
    "vscode-languageclient": "^10.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "@types/vscode": "^1.96.0",
    "@vscode/test-electron": "^2.5.2",
    "@vscode/vsce": "^3.7.0",
    "esbuild": "^0.27.2",
    "mocha": "^11.7.5"
  },
  "contributes": {
    "languages": [
      {
        "id": "llvm",
        "aliases": [
          "LLVM IR",
          "llvm"
        ],
        "extensions": [
          ".ll"
        ],
        "configuration": "./language-configuration.json"
      }
    ],
    "grammars": [
      {
        "language": "llvm",
        "scopeName": "source.llvm",
        "path": "./syntaxes/llvm.tmLanguage.json"
      }
    ]
  },
  "activationEvents": [
    "onLanguage:llvm"
  ],
  "engines": {
    "vscode": "^1.96.0"
  }
}
