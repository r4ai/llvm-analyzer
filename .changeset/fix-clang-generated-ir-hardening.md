---
"llvm-analyzer-vscode": patch
---

Fix clang-generated LLVM IR handling for debug metadata, multiline EH instructions, named struct GEP operands, and attribute type arguments. Also require VS Code Workspace Trust for external verifier settings and restrict document links to in-workspace files.
